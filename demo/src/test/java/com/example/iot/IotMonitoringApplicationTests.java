package com.example.iot;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Standard test class to verify that the Spring Application context loads correctly.
 */
@SpringBootTest
class IotMonitoringApplicationTests {

    /**
     * Context load check.
     */
    @Test
    void contextLoads() {
        // Verifies that the context starts up correctly without throwing exceptions.
    }
}
